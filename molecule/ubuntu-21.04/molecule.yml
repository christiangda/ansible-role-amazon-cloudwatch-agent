---
dependency:
  name: galaxy

driver:
  name: podman

provisioner:
  name: ansible
  # log: true

verifier:
  name: ansible

scenario:
  name: ubuntu-21.04

platforms:
  - name: ubuntu-21.04
    image: ubuntu:21.04
